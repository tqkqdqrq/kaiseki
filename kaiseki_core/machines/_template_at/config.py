"""AT機テンプレ用の機種パラメータ。

⚠️ 仮値禁止: 実機ごとに必ず公式仕様 (DMM, ちょんぼりすた等) から確定値を入れること。
   コインもち未確定なら ImportError 等でエラー化して、計算実行を防ぐ運用も検討。

スペック取得手順 (新機種):
1. `/dmm-ingest` で機種ページ取得 (vault に発表解析md保存)
2. ちょんぼりすたで「コインもち (G/50枚)」と「AT純増 (枚/G)」を確認
3. ユーザーに承認を取ってから本ファイルに反映
4. INVEST_PER_G = 50.0 / コインもち
"""

NORMAL_G_BREAK = 150       # 通常時消化G数しきい値 (実機調整)
COIN_HOLD_G_PER_50 = 30.0  # 通常時コインもち [G/50枚] (実機調整)
INVEST_PER_G = 50.0 / COIN_HOLD_G_PER_50  # ≒ 純減枚/G
AT_PAYOUT_PER_G = 2.8      # AT中純増 (参考)
STREAK_MIN_LENGTH = 2      # 連荘判定の最小チェーン長
