# 解析プロジェクト

パチスロ実践値解析。生CSV → `kaiseki_core` で状態判定・連荘検出・期待値表生成 → Excel/ダッシュボード出力。

## 機種汎用化の鉄則
- **「有利区間は差枚2400枚で完走」はほぼ全スマスロ共通**。だが **信号の上がり方・連荘の仕方・切断契機は機種ごとに全然違う**。
- 新機種追加時は、まず以下3点を機種ごとに確定してから adapter を書く。ビッグドリームのロジックをそのままコピーしない:
  1. **切断契機** (有利区間が切れる条件: 上位AT終了 / 天国終了 / 規定G完走 など)
  2. **信号の意味** (どの種別が CZ / AT / 上位 か)
  3. **連荘の継続条件** (どこまでを1連荘とするか)

## 構造
- `kaiseki_core/` — 機種非依存のコア (state判定基盤, 期待値表生成, ダッシュボード)
- `kaiseki_core/machines/<機種>/` — 機種固有 adapter / state / config / sheets
- `<機種>解析Python/解析実行.py` — 機種別の実行ラッパー (生CSV→xlsx)

## 機種別の用語・判定基準
- **ビッグドリーム**: 用語・判定ロジックの詳細は `kaiseki_core/machines/bigdream/解析仕様.md` を読むこと (上位AT判定 1000/2300/3000枚、引き戻し26-80G、TY=raw_payout、2400完走の扱い 等)。
- 普遍的なパチスロ用語の深掘りは Obsidian wiki `全機種ナレッジ/概念/` 参照 (別場所、必要時 Read)。
